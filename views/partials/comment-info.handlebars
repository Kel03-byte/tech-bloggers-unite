{{!-- Individual Comment Layout --}}

{{#each this}}
<div id='comment-card'>
    <div>
        <p id='comment-content'>{{comment_text}}</p>
        <p id='comment-info'>- {{user.username}}, {{format_date created_at}}</p>
    </div>
</div>
{{/each}}